### ENTRNCE Helm Repository


# Add helm chart
```bash
helm package my-app-chart 
```
This will create tgz file with chart in current directory

# Add helm chart to index
```bash
helm repo index .
```

# Update helm
```bash
helm repo update
```
